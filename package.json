{
  "name": "matheinstein",
  "version": "0.3.0",
  "private": true,
  "description": "MathEinstein — formula training system (MVP) with live teacher/student relay.",
  "scripts": {
    "start": "node server.js",
    "dev":   "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
